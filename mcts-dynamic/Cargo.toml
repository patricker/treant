[package]
name = "mcts-dynamic"
version = "0.1.0"
edition = "2021"
description = "Runtime-polymorphic adapter for the mcts crate, enabling language bindings."

[dependencies]
mcts = { path = ".." }
rand = { version = "0.8", features = ["small_rng"] }

[dev-dependencies]
serde_json = "1"
