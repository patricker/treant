# Phase 7 — "Vanguard" (hidden-rank duel, L'Attaque-derivative) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. THREE implementer tasks (7-1 rules engine, 7-2 determinized-PIMC AI, 7-3 tile/UI), sequential, each reviewed; critic checkpoint 10; push. Master-plan Global Constraints + ALL standing program rules apply (verification non-optional; sourced rules quoted in code; single-sourced encodings + round-trip; 5A hidden-info secrecy gates; i18n ×6; wasm rebuild flow; audit; guards; hand-set difficulty per research; explicit-path commits with trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; never push — the controller pushes at the gate).

**Goal:** The third and hardest hidden-info game: a hidden-rank capture-the-Standard duel on the public-domain L'Attaque mechanics (1908, sourced), at reduced 8×8/12-piece scope, with a determinized-PIMC never-peeks AI at hand-set difficulty. Research + all sourced quotes + the piece table + risk register: `plans/2026-07-12-ambush-research.md` (READ IT — §4 is the rule spec, §5 the AI approach, §6 the reuse map).

**NAMING (hard rules):** "Stratego" NEVER user-facing (Jumbo trademark; header-comment citations fine). "Ambush" REJECTED (Ambush!, Avalon Hill 1983 — research §2). **Final name: "Vanguard" — decided by Peter 2026-07-12.** Engine module `vanguard.rs`, wasm export `VanguardWasm`, tile id `vanguard`. Task 7-3 still runs the live-mark sniff test (BGG search + USPTO/EUIPO tabletop-class glance) on "Vanguard" before it goes user-facing and records the evidence.

## Task 7-1 — rules engine (`treant-wasm/src/vanguard.rs`), no AI yet

Two-phase hidden-info engine per research §4. WebFetch the L'Attaque Wikipedia page yourself and quote the load-bearing combat/mine/spy/scout sentences in the header (sourced-rules-or-blocked).

- **Ctor:** `new(size_preset: u32 /*0 = 6×7 mini, 1 = 8×8 standard*/, m1..m9-ish: army-composition counts for the 9 piece types per research §4 table (Marshal, Captain, Sergeant, Trooper, Scout, Sapper, Spy, Bomb, Standard), scout_long: u32 /*0/1*/)`. Feasibility: exactly 1 Standard required; total pieces ≤ setup-zone squares (mini: back 2 rows; standard: back 3 rows); clamp with documented policy + tests at extremes. Lakes: standard 8×8 = two 1×2 lakes on the middle rows; mini 6×7 = one 1×2 lake (fix exact squares in a header diagram; document — this is our design choice within attested L'Attaque lake mechanics).
- **Phases:** placement (each seat places its army into its setup zone via moves `"p:<piece-type>:<cell>"`, alternating seat 0 fully then seat 1 — simplest attested-compatible flow; blackout handles secrecy) then play (`"m:<from>:<to>"`). Display/parse single-sourced + round-trip incl. edge cells + rejection cases.
- **Movement/combat (verbatim-sourced):** movables step 1 orthogonal to empty; Scout any distance straight orthogonal through empties (if scout_long=1, else steps 1); attack = move onto enemy square; higher rank wins, loser removed, equal ⇒ both removed; Spy (rank 0) beats Marshal only when attacking; Sapper captures Bomb; Bomb destroys any non-Sapper attacker; Bomb/Standard immovable. **Combat publicly reveals both pieces' identities** — permanent public knowledge.
- **Anti-shuffle rules (needed for termination):** two-squares rule (a piece may not move back and forth between the same two squares on 3 consecutive own-turns) + a no-combat ply cap → Draw (reuse the 40-ply-style precedent, pick and document a cap suited to game length; test both).
- **Win:** capture enemy Standard; or opponent has no legal move (immobilisation). result() contract "" / "Draw" / seat digit; terminal_value signs per ARCADE.md.
- **Per-seat views (the 5A bar):** `get_board_for(seat)` = own pieces with ranks + enemy pieces as anonymous markers EXCEPT publicly-revealed ones (combat participants) which show rank + moved/scout-moved public flags if you choose to expose them (moved-ness is public knowledge — decide and document; it helps the UI). Opponent hidden ranks NEVER in the wrong seat's board string, legal_moves, or any debug output. `last_move_summary_for(seat)` = the combat-summary beat (public: "attacker X vs defender Y, Z removed") for the pre-handoff reveal.
- **Placeholder AI surface:** `weak_move` = random legal (7-2 replaces it); keeps the wasm wrapper complete and audit-registrable from day one.
- **Tests (~20 min):** placement validation (zones, counts, clamps), every combat rule (spy-attacks-marshal both directions, sapper-bomb, bomb-nonsapper, equal-rank mutual, scout long-move + blocked), reveal bookkeeping (combat participants public; movement flags), two-squares + draw cap, immobilisation win, per-seat secrecy (wrong seat's string free of hidden ranks — grep-style test), round-trip, full-game termination fuzz, audit `ok vanguard`.

## Task 7-2 — determinized-PIMC AI + perf spike

Research §5 is the spec. **Start with the timing spike:** implement the consistent-assignment sampler + a depth/playout-limited per-sample search, measure native ms/move at candidate (K, budget) points on a mid-game position, extrapolate wasm ≈3-5× native, and pick the Hard config that fits <1s/move; record the table in the report BEFORE polishing. If even K=8 × tiny search busts the budget, STOP and report BLOCKED with the numbers (descoping is the controller's call).

- **Sampler:** constructive/rejection assignment of the remaining hidden rank multiset onto the enemy's unrevealed piece positions, constrained by revealed identities, moved-piece flags (moved ⇒ not Bomb/Standard; 2+-straight ⇒ Scout), and multiset conservation. Reads ONLY public history + own secret.
- **Search per sample:** the sampled world is perfect-information — run a shallow treant search (or bounded rollouts; match whatever the repo's evaluator surface makes natural — study how salvo/other engines wire weak_move) and score root moves; **vote/average across K samples**; topK/temp sampling over the vote = the difficulty ladder (hand-set; register in calibrate.rs for the audit fuzzer; document the nim/bulls-cows precedent).
- **`vanguard_ai_never_peeks` test (genuine):** two games, different hidden enemy armies, identical public history + own pieces ⇒ identical sampled-candidate sets and identical move distribution at fixed seed. Plus: sampler-consistency test (every sample reproduces all revealed combats and movement evidence), and a behavioral sanity test (after the enemy Marshal is publicly revealed, the AI doesn't attack it with a known-weaker piece when a safe alternative exists — pick something honestly assertable; don't overfit).
- Full suite green, clippy 0, audit ok. Report the final ladder + ms/move table.

## Task 7-3 — tile + UI

- **NAME GATE FIRST:** the name is "Vanguard" (Peter, 2026-07-12); run the live-mark sniff test on it and record the evidence in the report. Tile id = lowercase name, category Family classics or Strategy (check Launcher CATEGORIES), `hiddenInfo: true`, `noUndo: true`, standalone. Knobs: size preset (mini/standard), army-composition editor (the 🙃 "eight bombs zero scouts" knob — respect engine clamps, surface clamped values honestly like Salvo's fleet editor), scout_long. Presets: Standard ⭐ (8×8/12), Mini ⚡ (6×7/8-piece composition — define in plan-consistent counts), Bomb Garden 🤯 (composition-editor silliness, exactly one 🤯), plus at most one more.
- **Placement UI (risk #3):** rank-palette tray (piece types with count badges + rank numbers), tap-to-place into the setup zone, tap-placed-to-pick-up, 🎲 auto-fill (engine random placement helper — add one in 7-3 ONLY if 7-2 didn't; else client-side mirroring Salvo's precedent with engine as validity oracle), 🔒 lock. Comfortable at 390px on 8×8.
- **Board:** own pieces show rank glyphs; enemy hidden pieces render as uniform backs; publicly-revealed enemy pieces show rank (persistently — public knowledge); moved-flag subtle marker if the engine exposes it; lakes visibly impassable; lastCells; dark palette vars.
- **Combat beat:** on any combat, both seats see the reveal (attacker vs defender, who fell) — in vs-AI inline, in pass-and-play as the pre-handoff summary (5A handoffSummary seam; both-seats-public so secrecy-safe). resultFlavor: Standard-capture win + immobilisation win ("boxed in") + draw-cap narration.
- **Verification (the 5A gates verbatim + game-specific):** production build + Playwright: full pass-and-play game with blackout coverage asserts; THE load-bearing grep — enemy hidden ranks absent from the DOM at the wrong seat at every phase incl. placement; revealed pieces DO show and persist; vs-AI Easy and Hard full games (Hard makes non-suicidal attacks post-reveal); noUndo; placement at 390px; composition-editor extremes clamp sanely; i18n ×6; guards; audit; wave-A spot-check.

## Gates

Reviews per task (opus): 7-1 = combat-rule fidelity vs quoted sources + secrecy trace + termination guarantees; 7-2 = sampler-consistency math + never-peeks genuineness + the perf table's honesty (re-run spot measurements) + ladder rationale; 7-3 = secrecy DOM gates re-run adversarially + placement UX cold + combat-beat clarity + name sniff-test evidence. Critic checkpoint 10: play it like a family (placement cold, mid-game reveals, endgame), try to cheat (blackout, DOM, summaries, revealed-persistence correctness), Easy/Hard feel + does the AI read as "thinking"?, Bomb Garden verdict, whatever nobody asked. Then the Phase-7 push.
